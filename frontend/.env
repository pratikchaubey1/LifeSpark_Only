# NOTE: This repo uses environment variables for both frontend and backend.
#
# Frontend (Vite)
# - Copy this file to `frontend/.env` for local dev (or set vars in your hosting platform)
VITE_API_URL=/api

# Backend
# - Copy this file to `.env` in the Backend folder and fill values.

# MongoDB connection string
MONGO_URI=mongodb://127.0.0.1:27017/life-spark-db

# Frontend origin allowed for CORS
CLIENT_ORIGIN=http://localhost:5173

# Gemini API key (DO NOT COMMIT REAL KEY)
GEMINI_API_KEY=your_gemini_key_here
